package ua.university.sms.service;import org.junit.jupiter.api.Test;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.context.SpringBootTest;import ua.university.sms.model.dto.EnrollmentDtos;import static org.junit.jupiter.api.Assertions.*;
@SpringBootTest class EnrollmentServiceTest{@Autowired EnrollmentService service;@Test void averageByCourseWorks(){assertNotNull(service.averageByCourse(1L));}@Test void missingStudentThrows(){assertThrows(RuntimeException.class,()->service.create(new EnrollmentDtos.Request(99999L,1L,true,90.0)));}}
