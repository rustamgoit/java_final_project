package ua.university.sms.controller;import org.junit.jupiter.api.Test;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;import org.springframework.boot.test.context.SpringBootTest;import org.springframework.http.MediaType;import org.springframework.test.web.servlet.MockMvc;import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;
@SpringBootTest @AutoConfigureMockMvc class StudentControllerTest{@Autowired MockMvc mvc;@Test void listStudentsReturnsOk() throws Exception{mvc.perform(get("/api/students")).andExpect(status().isOk());}@Test void createInvalidStudentReturns400() throws Exception{mvc.perform(post("/api/students").contentType(MediaType.APPLICATION_JSON).content("{\"fullName\":\"A\",\"email\":\"bad\",\"year\":0}")).andExpect(status().isBadRequest());}@Test void unknownStudentReturns404() throws Exception{mvc.perform(get("/api/students/999999")).andExpect(status().isNotFound());}@Test void swaggerEndpointWorks() throws Exception{mvc.perform(get("/v3/api-docs")).andExpect(status().isOk());}}
