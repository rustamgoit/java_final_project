package ua.university.sms.model.dto;import jakarta.validation.constraints.*;import java.time.*; public class EnrollmentDtos{public record Request(@NotNull Long studentId,@NotNull Long courseId,boolean paid,Double grade){} public record Response(Long id,Long studentId,String studentName,Long courseId,String courseTitle,boolean paid,Double grade,Instant enrolledAt){} public record PaymentRequest(boolean paid){} public record GradeRequest(@NotNull @DecimalMin("0.0")@DecimalMax("100.0") Double grade){} }
